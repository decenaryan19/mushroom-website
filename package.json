{
  "name": "mushroom-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy . --project-name=mushroom-website",
    "deploy:pages": "wrangler pages deploy . --project-name=mushroom-website",
    "preview": "wrangler pages dev ."
  },
  "devDependencies": {
    "wrangler": "^4.106.0"
  }
}
